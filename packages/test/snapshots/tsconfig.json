{
	"extends": "../../../common/build/build-common/tsconfig.node16.json",
	// gen-version is very particular and will only allow generation to `src/packageVersion.ts`
	// There is no production level code in this package. It is all test and tooling (run via mocha).
	"include": ["src/packageVersion.ts"],
	"compilerOptions": {
		"rootDir": "./src",
		"outDir": "./lib"
	},
}
